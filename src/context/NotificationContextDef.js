import { createContext } from 'react';

/**
 * NotificationContext - Context for managing in-app notifications
 * Provides access to user notifications and related functionality
 */
const NotificationContext = createContext();

export default NotificationContext;
